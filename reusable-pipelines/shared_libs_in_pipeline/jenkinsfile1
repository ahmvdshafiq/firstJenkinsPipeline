@Library('Jenkins_Pipeline_Shared_Libs') _

pipeline {
    agent any
    stages {
        stage('Audit Tools') {
            steps {
                script {
                    auditTools()
                }
            }
        }
    }
}
